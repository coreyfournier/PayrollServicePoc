apiVersion: dapr.io/v2alpha1
kind: Subscription
metadata:
  name: transfer-request-sub
spec:
  pubsubname: kafka-pubsub
  topic: transfer-requests
  routes:
    default: /api/Transfers/process-request
  metadata:
    rawPayload: "true"
